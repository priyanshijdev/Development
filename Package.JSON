{
  "name": "my-app1",  --> folder name
  "version": "0.1.0",  -> version 
  "private": true,    // this allows so that npm couldn't able to publish it.

  "dependencies": {
    "cra-template": "1.2.0", // create react app 
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-scripts": "5.0.1",
    "run": "^1.5.0",
    "start": "^5.1.0"
  },
  "scripts": {  
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "custom": "npm install && npm run start"  // && when user use command npm start custom ---> it will do npm i & start 
  }, 
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {   /// helps in prdocustioj
    "production": [ 
      ">0.2%",
      "not dead",   // not to use all dead 
      "not op_mini all"   // not to use opera mini alll browsers
    ],
    "development": [  /
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
