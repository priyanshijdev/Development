WS is Js api browsers provide for storing data locally and securely wihtin a users 
browsers.

Web Storage 
->Session Storage : 
->Local storage :

Session :
The session storage object (window.sessionStorage) stores data that persists for only 
one session of an opened tab.
When user reload the page then only stored data clear

Local : whatever gets stored in the window.localStorage object will not disappear during a reload or 
reopening of the web page or when users close their browsers. 
Those data have no expiration time. Browsers never clear them automatically.

The computer will delete the window.localStorage object’s content in the following instances only:

When the content gets cleared through Js(by manually).
When the browser’s cache gets cleared
